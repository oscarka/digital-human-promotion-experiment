# ============================================
# 统一环境变量配置文件
# ============================================
# 复制此文件为 config.env 并填入实际值
# 部署时只需修改这一个文件即可

# ============================================
# DockerHub 配置（必需）
# ============================================
# 你的 DockerHub 用户名（用于拉取镜像）
DOCKERHUB_USERNAME=your-dockerhub-username

# ============================================
# 域名和服务器配置
# ============================================
# 前端域名（部署后的访问地址）
DOMAIN=your-domain.com

# API 基础地址（前端访问后端的地址）
# 如果使用同一域名，可以设置为空（自动使用当前域名）
API_BASE_URL=

# WebSocket 基础地址（前端连接 WebSocket 的地址）
# 如果使用同一域名，可以设置为空（自动使用当前域名）
WS_BASE_URL=

# ============================================
# API Keys（必需）
# ============================================
# Google Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# 火山引擎配置（如果使用火山引擎语音识别）
# ============================================
VOLCANO_APP_KEY=your_volcano_app_key_here
VOLCANO_ACCESS_KEY=your_volcano_access_key_here
VOLCANO_SECRET_KEY=your_volcano_secret_key_here
VOLCANO_API_URL=wss://openspeech.bytedance.com/api/v3/sauc/bigmodel
VOLCANO_USE_PROXY=true

# ============================================
# 服务器端口配置（通常不需要修改）
# ============================================
TELEPHONE_SERVER_PORT=3002
PROXY_PORT=3001
NODE_ENV=production

# ============================================
# 使用说明
# ============================================
# 1. 复制此文件为 config.env
# 2. 填入所有必需的值（至少需要 GEMINI_API_KEY）
# 3. 如果使用同一域名部署前后端：
#    - 设置 DOMAIN=your-domain.com
#    - API_BASE_URL 和 WS_BASE_URL 留空（自动使用 DOMAIN）
# 4. 如果前后端分离部署：
#    - 设置 DOMAIN=frontend-domain.com
#    - 设置 API_BASE_URL=https://backend-domain.com
#    - 设置 WS_BASE_URL=wss://backend-domain.com
# 5. 运行部署命令即可
