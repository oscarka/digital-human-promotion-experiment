# 代理服务器 Dockerfile
FROM node:20-alpine

WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制代理服务器代码
COPY proxy-server.js ./

EXPOSE 3001

# 启动代理服务
CMD ["node", "proxy-server.js"]
